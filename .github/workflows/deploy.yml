name: CI/CD

on:
  push:
    branches: [main]  # or your desired branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: CI Build Step
      run: echo "Build succeeded!"  # Replace this with actual steps (e.g., build scripts)
